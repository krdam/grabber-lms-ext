# 🎨 Version 3.3.0 - Индивидуальные прогресс-бары и заголовки видео

## ✅ Что добавлено

### 1. Заголовки для каждого видео 📝

**Структура:**
```
┌─────────────────────────────────────────┐
│ Video Title from Page                   │  ← НОВОЕ!
│ HLS - 1080p                              │  ← НОВОЕ!
├─────────────────────────────────────────┤
│ [THUMBNAIL] 🎥 HTML5 Video               │
│    3:53     ⚡ HLS Stream                │
│             [Download HLS]               │
└─────────────────────────────────────────┘
```

**Откуда берутся заголовки:**
- **HTML5 Video**: `video.title` → `video.aria-label` → название страницы
- **Network: HLS**: `"HLS Stream - {название страницы}"`

---

### 2. Индивидуальный прогресс-бар для каждого видео 📊

**Было (v3.2.0):**
```
🎥 Videos Found on Page
  Video 1 [Download HLS]
  Video 2 [Download MP4]

📥 Active Downloads  ← Общий контейнер
  ▰▰▰▰▰▱▱▱▱▱ 50%
```

**Стало (v3.3.0):**
```
🎥 Videos Found on Page

Video Title 1
HLS - 1080p
  [THUMBNAIL]
  ⚡ HLS Stream
  
  ┌─────────────────────────────┐
  │ Downloading segments...     │  ← Прогресс ДЛЯ ЭТОГО видео
  │ ▰▰▰▰▰▱▱▱▱▱                  │
  │ 50% • Segment 15/30 • 45 MB │
  └─────────────────────────────┘
  
  [Download HLS] (скрыта во время загрузки)

Video Title 2
MP4 - 720p
  [THUMBNAIL]
  📥 Direct download
  
  ┌─────────────────────────────┐
  │ Downloading...              │  ← Прогресс ДЛЯ ЭТОГО видео
  │ ▰▰▰▰▰▰▰▰▱▱                  │
  │ 80% • 120 MB                │
  └─────────────────────────────┘
  
  [Download MP4] (скрыта во время загрузки)
```

**Преимущества:**
- ✅ Видно прогресс КАЖДОГО видео отдельно
- ✅ Можно скачивать несколько видео одновременно
- ✅ Прогресс рядом с видео (не нужно скроллить)
- ✅ Кнопка скрывается во время загрузки

---

### 3. Улучшенный UI видео 🎨

**Структура video item:**
```
┌─────────────────────────────────────────┐
│ Module 2: Technology Infusion           │ ← Заголовок
│ HLS - 1080p                              │ ← Подзаголовок
├───────────┬─────────────────────────────┤
│ [PREVIEW] │ 🎥 HTML5 Video • 3:53       │ ← Мета
│  120×67   │                              │
│   3:53    │ ⚡ HLS Stream: HLS           │ ← Info box
│           │ 🔊 Auto-merge with FFmpeg    │
│           │                              │
│           │ ╔═══════════════════════╗    │ ← Прогресс
│           │ ║ Downloading: 15/30    ║    │
│           │ ║ ▰▰▰▰▰▱▱▱▱▱ 50%       ║    │
│           │ ╚═══════════════════════╝    │
│           │                              │
│           │ [Download HLS]               │ ← Кнопка
└───────────┴─────────────────────────────┘
```

---

## 🔧 Технические изменения

### modules/video-detector.js

**Добавлено:**
```javascript
// Заголовок и подзаголовок
const videoTitle = video.title || `Video ${index + 1}`;
const videoSubtitle = video.format ? 
  `${video.format}${video.quality ? ` - ${video.quality}` : ''}` : '';

<div class="video-item-header-title">
  <div class="video-title">${videoTitle}</div>
  <div class="video-subtitle">${videoSubtitle}</div>
</div>

// Индивидуальный прогресс-бар
<div class="video-progress hidden" id="video-progress-${index}">
  <div class="progress-label" id="video-progress-label-${index}"></div>
  <div class="progress-bar">
    <div class="progress-bar-fill" id="video-progress-fill-${index}"></div>
  </div>
  <div class="progress-stats" id="video-progress-stats-${index}"></div>
</div>
```

**Извлечение заголовка:**
```javascript
const videoTitle = video.getAttribute('title') || 
                  video.getAttribute('aria-label') || 
                  document.title || 
                  `Video ${index + 1}`;
```

---

### modules/download-manager.js

**Изменена логика updateTaskUI:**
```javascript
updateTaskUI(task) {
  // Приоритет: индивидуальный прогресс-бар видео
  if (task.videoIndex !== undefined) {
    const videoProgressEl = document.getElementById(`video-progress-${task.videoIndex}`);
    
    if (videoProgressEl) {
      // Показываем прогресс
      videoProgressEl.classList.remove('hidden');
      
      // Обновляем прогресс-бар
      progressFill.style.width = `${task.progress}%`;
      progressLabel.textContent = task.message;
      progressStats.innerHTML = `${task.progress}% • Segment ${task.segment}/${task.totalSegments}`;
      
      // Скрываем кнопку во время загрузки
      downloadBtn.style.display = 'none';
      
      return; // НЕ используем глобальный контейнер
    }
  }
  
  // Fallback: глобальный контейнер (если videoIndex не передан)
  // ...
}
```

---

### popup.css

**Новые стили:**
```css
/* Заголовок видео */
.video-item-header-title {
  margin-bottom: 12px;
  padding-bottom: 8px;
  border-bottom: 2px solid #e0e0e0;
}

.video-title {
  font-size: 15px;
  font-weight: 600;
  color: #2c3e50;
}

.video-subtitle {
  font-size: 12px;
  color: #7f8c8d;
}

/* Индивидуальный прогресс-бар */
.video-progress {
  margin: 12px 0;
  padding: 12px;
  background: #f8f9fa;
  border-radius: 6px;
  border: 1px solid #e0e0e0;
}

.video-progress .progress-bar-fill {
  background: linear-gradient(90deg, #667eea 0%, #764ba2 100%);
}

.video-progress .progress-stats {
  font-size: 11px;
  color: #777;
  display: flex;
  justify-content: space-between;
}
```

---

### popup.js

**Передача videoIndex:**
```javascript
const result = await downloadManager.startSegmentedDownload(
  video.url, 
  filename, 
  video, 
  index  // ← Передаём индекс видео
);
```

**Добавление заголовка:**
```javascript
allVideos.push({
  title: `${resource.format} Stream - ${tab.title}`,  // ← Заголовок
  ...
});
```

---

## 📊 Сравнение

### До (v3.2.0):
```
🎥 Videos Found on Page

🎥 HTML5 Video • 3:53
⚡ HLS Stream: HLS
[Download HLS]

📥 Active Downloads
┌─────────────────────┐
│ Video downloading... │  ← Один общий прогресс
│ ▰▰▰▰▰▱▱▱▱▱ 50%     │
└─────────────────────┘
```

### После (v3.3.0):
```
🎥 Videos Found on Page

Module 2: Technology Infusion  ← Заголовок!
HLS - 1080p                     ← Подзаголовок!
┌──────────┬─────────────────┐
│ [PREVIEW]│ 🎥 HTML5 Video  │
│  120×67  │ ⚡ HLS Stream   │
│   3:53   │                 │
│          │ ┌─────────────┐ │ ← Прогресс ЗДЕСЬ!
│          │ │ 15/30 • 50% │ │
│          │ │ ▰▰▰▰▰▱▱▱▱▱  │ │
│          │ └─────────────┘ │
│          │ [Download HLS]  │
└──────────┴─────────────────┘
```

**Гораздо информативнее и удобнее!** ✨

---

## 🎯 Результат

**Version 3.3.0:**
- ✅ Заголовок для каждого видео
- ✅ Подзаголовок (формат + качество)
- ✅ Индивидуальный прогресс-бар
- ✅ Thumbnail для HTML5 Video
- ✅ Кнопка скрывается во время загрузки
- ✅ Можно скачивать несколько видео одновременно

**Красиво, информативно, удобно!** 🎨

---

## 🧪 Тестирование

**1. Перезагрузите расширение**

**2. Откройте страницу с видео**

**3. Проверьте:**
- Каждое видео имеет заголовок сверху
- При нажатии "Download HLS" прогресс-бар появляется прямо под видео
- Кнопка скрывается во время загрузки
- Можно скачивать несколько видео параллельно

**4. Проверьте консоль:**
```
✓ Found header via data-target: "Module 2: Technology..."
Processing 3 videos with titles
Video 0: "Module 2: Technology Infusion"
```

---

**Готово! Попробуйте скачать видео!** 🚀

