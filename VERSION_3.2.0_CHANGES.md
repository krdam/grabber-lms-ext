# 🚀 Version 3.2.0 - Автоматическое скачивание без открытия вкладок

## ✅ Что изменилось

### 🎬 Автоматическое извлечение Vimeo HLS

**Было (v3.1.x):**
```
Кнопка: "Open Video Page"
Процесс:
  1. Открывает Vimeo player в новой вкладке
  2. Пользователь вручную нажимает кнопку расширения там
  3. Скачивает видео
```

**Стало (v3.2.0):**
```
Кнопка: "📥 Extract & Download"
Процесс:
  1. Открывает iframe в ФОНОВОЙ вкладке (~2 сек)
  2. Автоматически захватывает HLS stream (~3 сек)
  3. Закрывает фоновую вкладку
  4. Сразу начинает скачивание
  5. Объединяет через FFmpeg (~30 сек)
```

**Итого:** ~35 секунд **БЕЗ** ручных действий! ⚡

---

### 📥 Прямое скачивание для видео с URL

**Было:**
- Открывало страницу → пользователь вручную скачивает

**Стало:**
- Попытка прямого скачивания через `chrome.downloads`
- Только если не получается → открывает страницу

```javascript
if (video.url && !video.requiresProcessing) {
  // Прямое скачивание
  await chrome.downloads.download({
    url: video.url,
    filename: 'video_1.mp4',
    saveAs: true
  });
}
```

---

## 🎨 UI изменения

### Кнопки:
- ✅ **"📥 Extract & Download"** - для Vimeo iframe (автоматически)
- ✅ **"📥 Download Video"** - для видео с прямым URL
- ✅ **"Download HLS"** - для HLS потоков
- ✅ **"Download MP4"** - для прямых файлов

### Info boxes:
```
🎬 Vimeo iframe: Auto-extract HLS (~5s)
⚡ Opens in background → captures stream → downloads
```

```
📥 Direct URL available
Will attempt direct download
```

---

## 📊 Workflow

### Vimeo iframe:
```
Нажатие кнопки
  ↓
Открывается iframe в фоне (invisible)
  ↓
background.js захватывает HLS URLs
  ↓
Закрывается фоновая вкладка
  ↓
Скачивание HLS сегментов (~30 сек)
  ↓
FFmpeg объединяет (~5 сек)
  ↓
✅ Готовый файл в Downloads!
```

**Никаких ручных действий!** 🎉

---

## 🔧 Технические детали

### Извлечение HLS из iframe:
```javascript
// Открыть iframe в фоне
const newTab = await chrome.tabs.create({ 
  url: video.iframeUrl,
  active: false  // Фоновый режим
});

// Подождать загрузки (5 секунд)
await new Promise(resolve => setTimeout(resolve, 5000));

// Получить захваченные HLS streams
const response = await chrome.runtime.sendMessage({
  action: 'getVideoUrls',
  tabId: newTab.id
});

const hlsStreams = response.resources.filter(r => r.type === 'hls');

// Закрыть вкладку
chrome.tabs.remove(newTab.id);

// Скачать HLS
downloadHLS(hlsStreams[0].url);
```

---

## ✨ Преимущества

### Для пользователя:
- ✅ **Никаких ручных действий** - всё автоматически
- ✅ **Не видит лишних вкладок** - открывается в фоне
- ✅ **Быстрее** - не нужно ждать открытия страницы и снова нажимать
- ✅ **Проще** - одна кнопка для всего

### vs. Старая версия:
| Действие | v3.1.x | v3.2.0 |
|----------|---------|---------|
| Vimeo iframe | Открыть → Нажать снова → Скачать | **Одна кнопка →  Готово!** |
| Прямой URL | Открыть → Скачать вручную | **Автоскачивание** |
| HLS stream | Скачать | Скачать |

---

## 🆚 Сравнение с Video DownloadHelper

| Функция | Video DownloadHelper | Nano Page Saver 3.2.0 |
|---------|---------------------|----------------------|
| Vimeo автоизвлечение | ✅ | ✅ |
| Фоновое открытие iframe | ✅ | ✅ |
| FFmpeg объединение | ✅ | ✅ |
| Прямое скачивание | ✅ | ✅ |
| HTML/PDF сохранение | ❌ | ✅ |

**Теперь на одном уровне!** 🚀

---

## 📝 Изменённые файлы:

- `popup.js` - Добавлена логика автоизвлечения HLS из Vimeo iframe
- `modules/video-detector.js` - Обновлены тексты кнопок и info boxes
- `manifest.json` - Версия 3.1.6 → 3.2.0

---

## 🎯 Результат

**Version 3.2.0 = Полностью автоматическое скачивание:**
- ✅ Vimeo iframe → Автоматически
- ✅ Прямые URL → Автоматически
- ✅ HLS потоки → Автоматически
- ✅ Никаких ручных открытий страниц!

**Как Video DownloadHelper, но удобнее!** 🎊

